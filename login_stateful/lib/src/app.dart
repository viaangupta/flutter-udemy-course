import 'package:flutter/material.dart';

class App extends StatelessWidget{
  Widget build(context) {
    return MaterialApp(
      title: 'Log me In',
      home: Scaffold (
        body: Text('Show me a form here!'),
      )
    );
  }
}